import { Layout } from "../../layout/layout";
import { AddVersionControl } from "../../components/versionControl/add";

export const AddVersionControlPage = () => {
  return (
    <Layout>
      <AddVersionControl />
    </Layout>
  );
};
